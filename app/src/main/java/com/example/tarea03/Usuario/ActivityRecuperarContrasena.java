package com.example.tarea03.Usuario;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;

import com.example.tarea03.R;

import java.lang.reflect.Parameter;

public class ActivityRecuperarContrasena extends AppCompatActivity {

    private EditText recuperar;

    private Button btnrecuperar;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_recuperar_contrasena);
/*
        btnrecuperar = findViewById(R.id.btnrecuperar);
        recuperar = findViewById(R.id.recuperar);


        btnrecuperar.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                validate();
            }
        });
    }


    public void validate(){

        String email = recuperar.getText().toString().trim();

        if(email.isEmpty() || !com.example.tarea03.Usuario.Pa)
    }


*/

    }}